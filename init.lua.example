-- Add these lines to  ~/.hammerspoon/init.lua  (create the file if it doesn't exist).
-- They tell Hammerspoon to load Claude Switchboard from this repo instead of from
-- ~/.hammerspoon, so the code can live with your other projects.
--
-- Adjust the path below if you cloned the repo somewhere other than ~/Git_Repos.

package.path = package.path .. ";" .. os.getenv("HOME") .. "/Git_Repos/claude-switchboard/?.lua"

local dd = require("desktop_dashboard")
dd.start()

-- Optional, but worth it: the command-line bridge. With these two lines you can
-- query the RUNNING instance from a shell, which is the only way to see why a
-- Desktop got the label it did without guessing:
--
--   hs -c "return dd.version"
--   hs -c "return hs.inspect(hs.spaces.allSpaces())"
--   hs -c "dd.saveLayout()"   then read ~/.hammerspoon/desktop_dashboard_state.json
--
-- require("hs.ipc")   -- put this ABOVE dd.start()
-- _G.dd = dd

-- Alternative to the package.path line above: symlink the file into place, e.g.
--   ln -s ~/Git_Repos/claude-switchboard/desktop_dashboard.lua ~/.hammerspoon/desktop_dashboard.lua
-- and then just:  local dd = require("desktop_dashboard"); dd.start()
